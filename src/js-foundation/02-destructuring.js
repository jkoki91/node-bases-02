

// console.log(process);
// console.log(process.env);

const { SHELL, HOMEBREW_PREFIX, npm_lifecycle_script } = process.env;

// console.table({SHELL, HOMEBREW_PREFIX, npm_lifecycle_script});

const charactesrs = ['Goku', 'Vegeta', 'Trunks'];

const [ , , trunks ] = charactesrs;

// console.log(trunks);
